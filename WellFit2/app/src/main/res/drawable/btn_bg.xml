<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color del botón -->
    <solid android:color="#C30000" />

    <!-- Bordes redondeados -->
    <corners android:radius="12dp" />

    <!-- Padding interno -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />

    <!-- Sombra ligera -->
    <size android:height="48dp" />

</shape>
